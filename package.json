{
  "name": "defenseless-booru",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "sudo docker build . -t 'registry.hamptonmoore.com/quotebook' && sudo docker push 'registry.hamptonmoore.com/quotebook'"
  },
  "author": "Hampton Moore",
  "license": "MIT",
  "dependencies": {
    "@databases/sqlite": "^3.0.0",
    "express": "^4.17.1",
    "express-handlebars": "^5.3.2"
  }
}
